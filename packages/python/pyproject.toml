[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "banodoco-workspace-client"
version = "0.1.0"
description = "Generated neutral client for Banodoco Workspace Runtime workspace.v1"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
